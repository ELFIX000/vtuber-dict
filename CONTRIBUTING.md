# 貢献ガイドライン (Contributing Guidelines)

本プロジェクトは、VTuberの名前を各種IMEでスムーズに一発変換できるようにするための**オープンソース辞書プロジェクト**です。

## ⚠️ クリーンルーム開発に関する最重要方針
本プロジェクトは、完全なクリーンルーム開発（Clean-Room Design）で構築・運用されています。
法的な透明性と健全性を維持するため、以下のルールを厳格に遵守してください。

1. **市販・有償辞書および他者作成辞書データの転載禁止**
   - BOOTHや同人誌等で販売・配布されている外部辞書データの持ち込み、コピー、抽出、比較対照は**一切禁止**します。
2. **一次情報（公式ソース）の明記**
   - 単語を追加・修正する際は、必ず**公式プロフィール、YouTubeチャンネル、所属事務所の公式サイト、またはWikidata/Wikipedia**等の公的かつ信頼できる一次ソースを `source` に記載してください。
3. **日常語との衝突回避**
   - 2文字〜3文字の極端に短い読み（例：「あい」「あめ」「つき」など）は、一般文のタイピング時に誤変換を頻発させるため、単体登録を避けるか `data/blacklist.txt` を確認してください。

---

## データの追加・修正手順

本プロジェクトでは、月次の自動収集（Wikipedia / Wikidata）による意図しない上書きを防ぐため、**手動での修正・追加は原則として `data/overrides.json` に登録**します。ここに登録されたデータは最優先され、自動収集パイプラインでも永続的に保護されます。

1. 本リポジトリをフォークします。
2. `data/overrides.json` にエントリを追加・編集します。
   ```json
   {
     "name": "正式名称（表記ママ）",
     "readings": ["ひらがなふるねーむ"],
     "affiliation": "所属事務所または個人勢",
     "category": "人名",
     "note": "修正理由や公式ソースURL（例: Issue #123 読み誤りの修正）"
   }
   ```
3. ローカルでバリデーションとビルドを実行し、エラーがないことを確認します。
   ```bash
   python3 src/validator.py
   python3 src/builder.py
   ```
4. プルリクエストを作成してください。CIにより自動でバリデーションが行われます。

> **Note**: Issueからの修正リクエスト（[Issues](../../issues)）も大歓迎です！申請いただいた内容はメンテナまたはAIによって `data/overrides.json` に反映され、確実に保護されます。
