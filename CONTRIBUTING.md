# 貢献ガイドライン (Contributing Guidelines)

本プロジェクトは、VTuberの名前を各種IMEでスムーズに一発変換できるようにするための**オープンソース辞書プロジェクト**です。

## 登録ルールと注意点

健全なオープンソース辞書として維持するため、以下のルールを遵守してください。

1. **他者の有償辞書データ等の無断転載禁止**
   - BOOTHや同人誌等で販売・配布されている外部辞書データの持ち込みやコピーは行わないでください。
2. **一次情報（公式ソース）の確認**
   - 単語を追加・修正する際は、公式プロフィール、YouTubeチャンネル、所属事務所の公式サイト等で表記と読みを確認してください。
3. **日常語との衝突回避**
   - 2文字〜3文字の極端に短い読み（例：「あい」「あめ」「つき」など）は、一般文のタイピング時に誤変換を頻発させるため、単体登録を避けるか `data/blacklist.txt` を確認してください。

---

## データの追加・修正手順

1. 本リポジトリをフォークします。
2. `data/overrides.json`（または `data/vtubers.json`）にエントリを追加・編集します。
   ```json
   {
     "name": "正式名称（表記ママ）",
     "readings": ["ひらがなふるねーむ"],
     "affiliation": "所属事務所または個人勢",
     "category": "人名",
     "note": "修正理由や公式ソースURL（例: Issue #123）"
   }
   ```
3. ローカルでバリデーションとビルドを実行し、エラーがないことを確認します。
   ```bash
   python3 src/validator.py
   python3 src/builder.py
   ```
4. プルリクエストを作成してください。CIにより自動でバリデーションが行われます。

> **Note**: Issueからの追加・修正リクエスト（[Issues](../../issues)）も歓迎しています。テンプレートに沿って申請してください。
